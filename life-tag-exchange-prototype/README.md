# 人生标签交易所：牛马肉铺

HTML 快速验证原型，用于验证首版配置、核心流程边界和后续 TypeScript 规则模块组织。

## 当前开发阶段

P0-1：可测试开发护栏。

本阶段只确保项目能运行、能构建、页面不白屏、配置加载状态可见、顶部状态栏可见、启动失败时有可读错误兜底。

## 当前已实现

- Vite / TypeScript 项目可启动。
- 配置加载。
- 配置校验。
- 配置索引。
- 初始运行状态创建。
- 顶部状态栏显示。
- 配置加载摘要显示。
- P0-1 阶段提示。
- 日志区。
- “开始新局”占位按钮与事件绑定。
- 配置或程序加载失败时显示 `fatal-error`。

## 当前未实现

- 每日阶段推进。
- 市场新闻生成。
- 商品候选生成。
- 顾客订单生成。
- 进货。
- 抽牌。
- 交易预览。
- 售价计算。
- 爆雷计算。
- 事故结算。
- 基础操作效果。
- 收店奖励。
- 最终报告。
- 存档。
- Debug 测试局。

## 运行命令

```bash
npm install
npm run check
npm run build
npm run dev
```

预览生产构建：

```bash
npm run preview
```

## 手动验收步骤

1. 运行 `npm install`。
2. 运行 `npm run check`，确认 TypeScript 检查通过。
3. 运行 `npm run build`，确认生产构建通过。
4. 运行 `npm run dev`。
5. 打开终端输出的本地地址，通常是 `http://127.0.0.1:5173/` 或 `http://localhost:5173/`。
6. 确认页面显示游戏标题“人生标签交易所：牛马肉铺”。
7. 确认页面显示“当前开发阶段：P0-1：可测试开发护栏”。
8. 确认页面显示“配置加载成功”和配置表数量。
9. 确认顶部状态栏显示阶段、天数、现金、累计利润、信誉、行动点、库存、手牌数量。
10. 确认页面存在“开始新局”按钮。
11. 点击“开始新局”，确认日志区新增“P0-1 阶段尚未实现完整新局流程。”。
12. 打开浏览器控制台，确认没有阻塞级红色错误。

## 常见问题

### 页面白屏怎么办

先打开浏览器控制台查看错误。如果页面中出现“配置或程序加载失败”，优先按错误 message 检查配置文件路径、JSON 格式或配置校验问题。

### 配置加载失败怎么办

确认 `data/` 下配置文件名与 `src/core/configLoader.ts` 一致。当前奖励文件使用 `rewards.json`，不是 `rewardPools.json`。

### npm run build 失败怎么办

先运行 `npm run check` 查看 TypeScript 错误，再根据终端中的文件路径和行号修复。配置口径错误通常会在页面启动时由 `validateConfigs(configs)` 抛出。

## 分层原则

- `data/` 只放配置。
- `src/core/` 只放规则、状态、数据加载、校验、索引、类型。
- `src/ui/` 只放显示、按钮、DOM 事件。
- `src/main.ts` 只负责加载配置、校验、建索引、创建状态、调用 render、绑定事件。
